#include <stdio.h>
int main()
{
    int a,b,sum;   // Declare integer variables
    a = 5;           // Assign value a=5
    b = 8;           // Assign value b=8
    sum = a+b;       // Add a and b ,store in sum
     
    printf("sum = %d",sum);   // Print the result

    return 0;
}